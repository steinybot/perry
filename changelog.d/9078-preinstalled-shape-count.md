### Fixed

- Preinstalled class ShapeIds now verify the canonical keys array's physical key count before stamping a newborn, falling back to exact shape publication when the pointer's facts have drifted.
- The real npm-library integration suite now honors CI's production release runtime and builds a release static runtime locally, preserving Perry's shipped JavaScript exception transport.
